version: "3.5"
services:
    rabbitmq:
        image: "rabbitmq:3-management"
        ports: 
            - 15672:15672
            - 5672:5672
    gaia:
        build: .
        environment: 
            RABBITMQ_ADDR: rabbitmq
        ports: 
            - 8080:8000